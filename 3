<!DOCTYPE html>
<html>
<head>
    <title>Convert JSON Data to HTML Table Example</title>
    <meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
    <style>
        @media (max-width: 960px) {
            div {
            margin: 10px 100px 30px 100px;
            }
        }
        @media(min-width: 960px) {
            div {
                margin: 10px 300px 30px 300px;
            }
            p {
            font:15px Verdana;
            }
        h1 {
            font-weight:bold;
            font:15px Verdana;
        }
        h2 {
            font-weight:bold;
            font: 15px Verdana;
            }
        }
        table, th, td  
        {
            margin:10px 0;
            border:solid 1px #333;
            padding:2px 4px;
            font:15px Verdana;
        }
        th {
            font-weight:bold;
        }

        
    </style>
</head>
<body>
    <div>
        <h1>How to Use Shulgi</h1>
        <h2>Removing New Lines from Clipboard Contents</h2>
        <p>
        To retrive text that has been copied into the system clipboard, click on the
        extensions icon. Then on the extensions popup menu click the button that says
        "Get Translation". A new tab will be generated with the text in Google Translate. 
        </p>
        <h2>Using the Look-up Feature</h2>
        <p>
        To look up a word you can type it into the text box and hit the return key, or
        the button marked "Define". If no language code is specified, a new tab will
        be generated defining it in Google Translate. You can specify a language using a code, 
        most of the codes are based on
        <a href="https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes">ISO langauge codes</a>, 
        but some are based on the web page name, or convenient pneumonic. Users, can chose their
        own codes, but it is avaisable to adhere closely to ISO codes. When a languages is specified 
        a tab will be generated in the dictionaries that
        have been defined in the config. This chrome extension currently supports:
        German (de), Korean (ko), French (fr), Hebrew (he), Chinese (zh), 
        Spanish (sp), and others. To use the language codes, seperate the language code from the
        search term with a colon(:). Like: <tt>de: Hallo</tt>, or <tt>sp: hola</tt>. 
        </p>
        <p>
            <button id="CreateTable">Show Codes</button>
            <div id="showData"></div>
            <div id="tablePrint"></div>
        </p>
        <h2>Keyboard Shortcuts</h2>
        <p>
        Shulgi also supports keyboard shortcuts. On Windows <tt>Ctrl+Shift+U</tt> will open
        the popup html file. And <tt>Ctrl+Shift+Y</tt> will open Google Translate with
        whatever is in the clipboard. For Mac, also use the <tt>Ctrl</tt> key. Both of these
        keyboard shortcuts can by combined: Hold down <tt>Ctrl+Shift</tt>, and then press <tt>U</tt> 
        and then <tt>Y</tt> and Shulgi will open the Google Translate page. 
        </p>
        <h2>Add Codes and URLs</h2>
        <p>
        You can use any code you would like. It would be best to chose somehting that you can 
        remember, also if you chose one of the existing codes it will overwrite that url.
        To format the url use <tt>{{s}}</tt> where the search term is placed in the url. For example,
        you could add DuckDuckGo like: <tt>http://duckduckgo.com/?q={{s}}</tt>. Note some websites will
        place the search term in the middle of a url, and someother websites handle variables in 
        a way that will not facilitate being used by this extension.
        </p>
        <p>
        Code:<input id="ISOcode" type="text" size=1 ></input>
        URL:<input id="URLwithSubString" type="text" size=40></input>
        <button id="AddPairToConfig">Add to Config</button>
        </p>
        </div>
</body>
<script src="config.js"></script>
<script src="help.js"></script>
</html>
